import { Meta } from "@storybook/blocks";

<Meta />

# Inputs

Inputs are used to collect data from the user. Inputs without the postfix `Validated` are not validated and can be used to compose new inputs.
The `*Validated` inputs using the validation library [vee-validate](https://vee-validate.logaretm.com/v4/) to validate the input.

