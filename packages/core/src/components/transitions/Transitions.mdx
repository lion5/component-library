import { Meta } from '@storybook/addon-docs/blocks'

<Meta title="Transitions" />

# Transitions

Reusable Vue `<Transition>` wrappers that provide a consistent animation for showing/hiding content,
regardless of what that content is. They're generic building blocks, not tied to any specific domain
category.

Example: `ScaleTransition`.

